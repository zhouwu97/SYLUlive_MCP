[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "sylulive-mcp"
version = "0.4.0"
description = "面向 SYLUlive Agent 的纯工具型 MCP Server"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "zhouwu97" }]
dependencies = [
  "httpx>=0.28,<1",
  "mcp==1.28.1",
  "pydantic>=2.10,<3",
  "pydantic-settings>=2.7,<3",
  "starlette>=0.47,<1",
  "tzdata>=2025.1",
  "uvicorn>=0.35,<1",
]

[project.scripts]
sylulive-mcp = "sylulive_mcp.__main__:main"
hy3-campus-decision-mcp = "hy3_campus_decision_mcp.__main__:main"

[dependency-groups]
dev = [
  "pytest>=8.3,<9",
  "pytest-asyncio>=0.24,<1",
  "ruff>=0.8,<1",
]

[tool.hatch.build.targets.wheel]
packages = ["src/sylulive_mcp", "src/hy3_campus_decision_mcp"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
