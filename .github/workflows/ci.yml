name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]
    env:
      SYLULIVE_MCP_MODE: demo
      SYLULIVE_DEMO_ROOT: ./examples
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          python-version: "${{ matrix.python-version }}"
      - run: uv sync --frozen
      - run: uv run ruff check .
      - run: uv run ruff format --check .
      - run: uv run pytest -q
      - run: uv run python scripts/selfcheck.py
      - run: uv run python scripts/sdk_stdio_client.py
      - run: uv run python scripts/sdk_streamable_http_client.py
      - run: uv run python scripts/verify_sylulive_contract.py
